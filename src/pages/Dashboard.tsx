import React from "react";
import MainLayout from "../components/MainLayout";

const Dashboard = () => {
  return (
    <MainLayout>
      <div className="w-full bg-white h-screen flex justify-center items-center">
        <h1 className="text-[5rem] ">Dashboard</h1>
      </div>
    </MainLayout>
  );
};

export default Dashboard;
